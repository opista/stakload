{
  "name": "@stakload/nestjs-logging",
  "private": true,
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint --cache .",
    "lint:fix": "eslint --cache --fix .",
    "typecheck": "tsc --noEmit --incremental false -p tsconfig.json --composite false"
  },
  "dependencies": {
    "nestjs-pino": "^4.6.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@nestjs/common": "^11.1.14",
    "@nestjs/core": "^11.1.18",
    "@stakload/eslint-config": "workspace:*",
    "rimraf": "^6.0.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.1.14",
    "@nestjs/core": "^11.1.18"
  }
}
